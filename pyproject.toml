[project]
name = "datadoctor-llm-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chokunnotlikeus", email = "ittikorn.chokun@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "gradio>=6.28.0",
    "langchain>=1.4.2",
    "langchain-community>=0.4.2",
    "langchain-core>=1.6.4",
    "openpyxl>=3.1.5",
    "pandas>=3.0.6",
    "unstructured>=0.27.8",
]

[project.scripts]
datadoctor-llm-project = "datadoctor_llm_project:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
